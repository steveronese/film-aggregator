import { defineConfig } from 'astro/config';

// Static output (default). Set `site` to the real domain at deploy time for correct canonical URLs.
export default defineConfig({
  site: 'https://example.com',
});
